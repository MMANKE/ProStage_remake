{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier Entreprise</h1>

    {{ include('entreprise/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="row mx-auto">
      <div class="mx-1">
        <a class="btn btn-primary" href="{{ path('entreprise_index') }}" role="button">Retour à la liste</a>
      </div>
      <div class="mx-1">
        {{ include('entreprise/_delete_form.html.twig') }}
      </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}

  <!-- nom -->
  <h1>
    {{entreprise.nom}}
  </h1>

  <!-- Activité -->
  <div class="card">
  <div class="card-header">
  Activité :
  </div>
  <div class="card-body">
    {{entreprise.activite}}
  </div>
  </div>

  <!-- Adresse -->
  <div class="card">
  <div class="card-header">
    Informations :
  </div>
  <div class="card-body">
  <div class="card">
    <div class="card-body">
      Adresse : {{entreprise.adresse}}
    </div>
  </div>
  <div class="card">
    <div class="card-body">
      Site web : <a href="{{entreprise.siteWeb}}">{{entreprise.siteWeb}}</a>
    </div>
  </div>
  </div>
  </div>
{% endblock %}

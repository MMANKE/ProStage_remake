{% extends 'base.html.twig' %}

{% block body %}

  <div class="my-3">
    <h1> Formation </h1>
  </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formation.nom }}</td>
            </tr>
            <tr>
                <th>Nom Complet</th>
                <td>{{ formation.nomComplet }}</td>
            </tr>
        </tbody>
    </table>
  <div class="row mx-auto">
<div class="mx-1">
      <a class="btn btn-primary" href="{{ path('formation_index') }}">Retour à la liste</a>
    </div>
{#  <div class="mx-1">
      <a class="btn btn-primary" href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier</a>
    </div> #}
    <div class="mx-1">
      {{ include('formation/_delete_form.html.twig') }}
    </div>
  </div>
{% endblock %}
